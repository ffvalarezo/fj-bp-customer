server:
  port: 8082

spring:
  application:
    name: account-service
  profiles:
    active: local
  
  h2:
    console:
      enabled: true
      path: /h2-console
  
  r2dbc:
    url: r2dbc:h2:mem:///account_db
    username: sa
    password: password

management:
  endpoints:
    web:
      exposure:
        include: health,info
